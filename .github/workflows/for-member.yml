name: Member

on:
  pull_request:
    branches:
    - 'dev'

jobs:
  php-cs-fixer:
    uses: ./.github/workflows/phpcsfix.yml
  composer-optimize:
    needs: php-cs-fixer
    uses: ./.github/workflows/opetimize-composer.yml
